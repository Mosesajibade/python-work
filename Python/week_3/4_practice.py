class Mammal:
    kind = 'human'
    def __init__(self,name):
        self.name = name
ma = Mammal('Moses')
mk = Mammal('Madina')

print("ma.kind", ma.kind)
print("mk.kind", mk.kind)
print("ma.name", ma.name)
print("mk.name", mk.name)



# first thing we want to do is to define the class.
# then put the kind and then use the init function using "def" which is used to create a new object, them "self.name = name" to set up the name.
# then we set a vairable with the two people or objects we want to use. 
# then we print the kind and name



class boys:
    kind = "Ajiabde"
    def __init__ (self, name):
        self.name = name
ma = boys("moses")
sa = boys("samson")
pa = boys("peter")
paa = boys("paul")

print("ma.kind", ma.kind)
print("sa.kind", sa.kind)
print("pa.kind", pa.kind)
print("paa.kind", paa.kind)
print("ma.name", ma.name)
print("sa.name", sa.name)
print("pa.name", pa.name)
print("paa.name", paa.name)


